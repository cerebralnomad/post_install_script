
colorscheme molokai         " awesome colorscheme git@github.com:tomasr/molokai.git

syntax enable           " enable syntax processing

set tabstop=4       " number of visual spaces per TAB

set softtabstop=4   " number of spaces in tab when editing

set expandtab       " tab inserts 4 spaces

nnoremap o o<Esc>   " return to command mode after inserting new line below
nnoremap O O<Esc>   " return to command mode after inserting new line above

set number              " show line numbers

set showcmd             " show command in bottom bar

filetype indent on      " load filetype-specific indent files

set showmatch           " highlight matching [{()}]

set incsearch           " search as characters are entered
set hlsearch            " highlight matches


